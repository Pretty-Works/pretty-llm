# 이 파일을 복사해 .env 로 만들고 실제 값을 채운다. (.env 는 git에 올리지 말 것)

# ── LLM ──
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4
LLM_API_KEY=여기에_실제_키
LLM_TIMEOUT_S=30
LLM_MAX_RETRIES=2

# ── 백엔드 연동 (AI가 데이터 되물을 때 쓰는 주소) ──
BACKEND_BASE_URL=http://localhost:8080

# ── 앱 ──
DEBUG=false
